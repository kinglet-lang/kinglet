Expand the self-hosted bytecode compiler coverage. Current state: compiler/compiler.kl supports int/bool literals, local vars, arithmetic + - * / %, var decls, return, function decls, function calls. 2 golden tests pass (just42, addmain).

GOAL: Add the following features, commit after each one is working (compiles + golden test passes against C++ output):

FEATURE 1: String literals
- Add string literal support to compile_expr and compile_literal
- Golden test: str.kl — int main() { string s = "hello"; return 0; }
- C++ truth: kinglet --bytecode str.kl (use tests/codegen/run_golden.sh for diff)

FEATURE 2: Comparison operators
- == != < > <= >= — all should map to Eq/Neq/Lt/Gt/Le/Ge opcodes
- Golden test: cmp.kl — int main() { return 1 == 2; } (should return 0/false)

FEATURE 3: If/else statements
- If/Else with JmpFalse/Jmp opcodes
- Golden test: ifelse.kl — int main() { if (true) { return 1; } else { return 0; } }

FEATURE 4: While loops
- While with Jmp/JmpFalse
- Golden test: while.kl — int main() { int i = 0; while (i < 5) { i = i + 1; } return i; }

FEATURE 5: Block expressions (trailing expr)
- Block of statements where last is an expression → that's the block's value
- Golden test: block.kl — int main() { { int x = 3; x + 7 } } (should return 10)

RULES:
- Commit after each feature: feat(compiler): ...
- Every golden test must diff byte-identical against C++ --bytecode output
- Reference C++ compiler at /home/sentomk/Code/kinglet-bootstrap/src/compiler/compiler.cc
- Bootstrap binary: /home/sentomk/Code/kinglet-bootstrap/out/Debug/kinglet
- Test command: cd /home/sentomk/Code/kinglet && KINGLET=/home/sentomk/Code/kinglet-bootstrap/out/Debug/kinglet bash tests/codegen/run_golden.sh
- If the bootstrap compiler emits warnings, ignore them — they're from the self-host code, not your problem
- If a feature requires emulating bootstrap quirks, document them as comments in compiler.kl
- Use the existing Compiler struct, emit helpers, and Chunk API — don't rewrite the whole module
