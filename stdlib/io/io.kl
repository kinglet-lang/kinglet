// Platform I/O — implementation: C ABI (libkinglet_rt).
// Checker reads this stub; codegen lowers to kl_io_* / VM NativeOut.
//
// Ergonomics (both retained long-term):
//   using io;              → qualified io::out.line(...)
//   using namespace io;    → unqualified out.line(...)

export module io;
