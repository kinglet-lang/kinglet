// stdlib/native/fs — platform filesystem binding (ADR 0003, layer L2).
//
// SCAFFOLD ONLY. See stdlib/native/io.kl for the migration status. The active
// API list lives in stdlib/native/manifest.kl (L1) until `extern native` lands.
//
// Intended declarations (syntax TBD, semantics fixed by ADR 0003):
//
//   extern native namespace fs {
//     string __read(string path);            // file contents, or null on error
//     void   __write(string path, string content);
//   }
