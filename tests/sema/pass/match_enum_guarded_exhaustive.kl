// RUN: check
enum Color { Red, Green, Blue, }

int score(Color c) {
  return c match {
    Color::Red => 1,
    Color::Green if (false) => 2,
    Color::Blue => 3,
  };
}

int main() {
  return score(Color::Red);
}
