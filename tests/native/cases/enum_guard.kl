enum Option {
  Some(int),
  None,
}

int main() {
  Option a = Option::Some(42);
  int result = a match {
    Option::Some(let x) if (x > 10) => 2,
    Option::Some(_) => 1,
    Option::None => 0,
  };
  return result;
}
