int main() {
  int x = 12;
  int r = x & 10;            // 8
  r = r + (x | 3);           // 23
  r = r + (x ^ 15);          // 26
  r = r + (~x + 14);         // 27
  r = r + (x << 2);          // 75
  r = r + (x >> 1);          // 81
  if (!false) { r = r + 1; } // 82
  return r;
}
