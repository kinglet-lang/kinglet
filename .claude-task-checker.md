Build the type checker module for the self-hosted Kinglet compiler. Lexer and parser are already complete (5 golden tests each, all passing). Now we need the checker.

CONTEXT:
- Bootstrap compiler: /home/sentomk/Code/kinglet-bootstrap/out/Debug/kinglet
- Self-hosted compiler: /home/sentomk/Code/kinglet (this repo)
- Existing modules: lexer/ (token.kl, scanner.kl, keywords.kl), parser/ (ast.kl, parser.kl), cli/main.kl
- CLI flags: --tokens and --ast already work
- Test command: /home/sentomk/Code/kinglet-bootstrap/out/Debug/kinglet cli/main.kl <test-file>

STEPS (commit after each):

STEP 1: Create checker/checker.kl
Type checker that walks AST, performs type inference:
- Let bindings with type inference from literal RHS
- Integer and string literal types
- Binary expressions (arithmetic → int, comparison → bool)
- Variable references (look up in scope stack)
- If expressions (branches must have same type)
- Block expressions (return type of last expr)
- Error reporting: file:line:col: <message> to stderr, return error count
Use a simple scope stack for variable bindings.

STEP 2: Wire --check flag in cli/main.kl
When --check is set: parse source, run checker on AST.
Print "OK: no type errors" on success, "N type error(s)" on failure. Exit 1 on errors.

STEP 3: Write golden tests
Create test/golden/checker/ with:
- pass_let.kl — let x = 42; (should pass)
- pass_infer.kl — let x = 42; let y = x + 1; (should pass, infer types)
- pass_scope.kl — inner let shadows outer (should pass)
- fail_mismatch.kl — let x: int = "hello"; (should fail)
Write a test runner script that runs each case through --check and verifies expected pass/fail.

STEP 4: Run all tests
Lexer, parser, AND checker tests must all pass.

RULES:
- Commit after each step: feat(checker): ... or fix(checker): ...
- Use bootstrap compiler to compile and test
- Import parser/ast.kl for AST types
- Match existing code style: pub fn, enum with payloads, exhaustive match
- Start with Step 1, do NOT rush everything at once.
